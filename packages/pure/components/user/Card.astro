---
import type { HTMLTag, Polymorphic } from 'astro/types'

import { cn } from '../../utils'

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag }> & {
  heading?: string
  headingHref?: string
  subheading?: string
  date?: string
  class?: string
}

const { as: Tag = 'div', class: className, href, heading, headingHref, subheading, date } = Astro.props
---

<Tag
  class={cn(
    'not-prose block relative card-radius border bg-muted px-5 py-3',
    href && 'transition-all hover:border-foreground/25 hover:shadow-sm',
    className
  )}
  href={href}
>
  <div class='flex flex-col gap-y-1.5'>
    <div class='flex flex-col gap-y-0.5'>
      <h2 class='text-lg font-medium'>
        {headingHref ? <a href={headingHref} class='card-heading-link'>{heading}</a> : heading}
      </h2>
      <p class='text-muted-foreground'>{subheading}</p>
      <p class='text-muted-foreground'>{date}</p>
    </div>
    <slot />
  </div>
</Tag>

<style>
  /* Driven by the shared --radius custom property (src/assets/styles/app.css)
     instead of a fixed Tailwind/UnoCSS rounded-* utility, so a single change
     to --radius adjusts corner-rounding sitewide, including here. */
  .card-radius {
    border-radius: calc(2 * var(--radius, 0.5rem));
  }

  .card-heading-link {
    color: inherit;
    text-decoration: none;
  }

  .card-heading-link:hover,
  .card-heading-link:focus-visible {
    text-decoration: underline;
  }
</style>
